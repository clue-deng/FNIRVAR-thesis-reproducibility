# Reproduction

- verify_checksums.py verifies every curated file listed in SHA256SUMS.txt.
- regenerate_figures.sh rebuilds figures from included result CSVs.
- FULL_REPRODUCTION_COMMANDS.md gives explicit commands for each simulation stage.

Quick integrity check:

    python reproduce/verify_checksums.py
